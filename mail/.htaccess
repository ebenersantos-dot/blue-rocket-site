# Deny direct browser access to this directory.
# contact.php handles its own CORS/origin checks so it remains callable
# via fetch() from the frontend — this just blocks directory listing
# and direct non-PHP access.

Options -Indexes

# Block everything except contact.php
<FilesMatch "^(?!contact\.php$)">
    Require all denied
</FilesMatch>
